// import { useMutation, useQuery } from "@apollo/client";
// import { TASKS_LIST_QRY } from "../queries";
// import {TaskTypeData} from './types'

// export const Jumper = () => {
//   const { loading, data, error } = useMutation<TaskTypeData>(TASKS_LIST_QRY);
//   if (loading) return <p>Loading...</p>;
//   if (error) return <p>Error</p>;
//   console.log(data?.tasksList);
//   console.log('From jumper');
  
// }

export{}
